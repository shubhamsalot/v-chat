{
  "rules": {
    "signaling": {
      "$matchId": {
        ".read": "auth != null",
        ".write": "auth != null"
      }
    },
    "matches": {
      "$matchId": {
        "messages": {
          ".read": "auth != null",
          ".write": "auth != null"
        }
      }
    }
  }
}
